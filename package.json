{
  "name": "scout-freedomplus-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "npm run build -w tokenGenerator "
  }
}
